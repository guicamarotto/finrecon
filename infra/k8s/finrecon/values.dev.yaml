# Overrides for dev environment (lower resources, single replicas)
api:
  replicas: 1
  resources:
    limits:
      cpu: 250m
      memory: 256Mi

worker:
  replicas: 1

frontend:
  replicas: 1

hpa:
  api:
    minReplicas: 1
    maxReplicas: 3
